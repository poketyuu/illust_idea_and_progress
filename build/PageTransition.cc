//this file is generated by program(drogon_ctl) automatically,don't modify it!
#include "PageTransition.h"
#include <drogon/utils/OStringStream.h>
#include <string>
#include <map>
#include <vector>
#include <set>
#include <iostream>
#include <unordered_map>
#include <unordered_set>
#include <algorithm>
#include <list>
#include <deque>
#include <queue>
using namespace drogon;
std::string PageTransition::genText(const DrTemplateData& PageTransition_view_data)
{
	drogon::OStringStream PageTransition_tmp_stream;
	std::string layoutName{""};
	PageTransition_tmp_stream << "<!DOCTYPE HTML>\n";
	PageTransition_tmp_stream << "<head>\n";
	PageTransition_tmp_stream << "<meta http-equiv=\"Refresh\" content=\"0;URL=";
{
    auto & val=PageTransition_view_data["newpage"];
    if(val.type()==typeid(const char *)){
        PageTransition_tmp_stream<<*any_cast<const char *>(&val);
    }else if(val.type()==typeid(std::string)||val.type()==typeid(const std::string)){
        PageTransition_tmp_stream<<*any_cast<const std::string>(&val);
    }
}
	PageTransition_tmp_stream << "\">\n";
	PageTransition_tmp_stream << "</head>\n";
	PageTransition_tmp_stream << "<body>\n";
	PageTransition_tmp_stream << "<main>\n";
	PageTransition_tmp_stream << "</main>\n";
	PageTransition_tmp_stream << "<script src=\"https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/js/bootstrap.bundle.min.js\" integrity=\"sha384-ka7Sk0Gln4gmtz2MlQnikT1wXgYsOg+OMhuP+IlRH9sENBO0LRn5q+8nbTov4+1p\" crossorigin=\"anonymous\"></script>\n";
	PageTransition_tmp_stream << "</body>\n";
	PageTransition_tmp_stream << "</html>\n";
if(layoutName.empty())
{
std::string ret{std::move(PageTransition_tmp_stream.str())};
return ret;
}else
{
auto templ = DrTemplateBase::newTemplate(layoutName);
if(!templ) return "";
HttpViewData data = PageTransition_view_data;
auto str = std::move(PageTransition_tmp_stream.str());
if(!str.empty() && str[str.length()-1] == '\n') str.resize(str.length()-1);
data[""] = std::move(str);
return templ->genText(data);
}
}
